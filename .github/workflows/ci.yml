name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: getmnemo/.github/.github/workflows/node-test.yml@main
    with:
      package-manager: npm
      node-version: "20"
