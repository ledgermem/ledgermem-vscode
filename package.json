{
  "name": "mnemo-vscode",
  "displayName": "Mnemo",
  "description": "Persistent memory for AI coding assistants. Search, add, and manage memories from VS Code, Cursor, Windsurf, and code-server.",
  "version": "0.1.0",
  "publisher": "getmnemo",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/getmnemo/getmnemo-vscode.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other", "AI", "Machine Learning"],
  "keywords": ["memory", "ai", "llm", "context", "rag"],
  "activationEvents": [
    "onCommand:getmnemo.search",
    "onCommand:getmnemo.add",
    "onCommand:getmnemo.deleteMemory",
    "onCommand:getmnemo.refresh",
    "onCommand:getmnemo.setApiKey",
    "onView:getmnemo.recentMemories"
  ],
  "main": "./out/extension.js",
  "icon": "media/icon.png",
  "contributes": {
    "commands": [
      {
        "command": "getmnemo.search",
        "title": "Mnemo: Search Memory",
        "category": "Mnemo"
      },
      {
        "command": "getmnemo.add",
        "title": "Mnemo: Add Selection to Memory",
        "category": "Mnemo"
      },
      {
        "command": "getmnemo.deleteMemory",
        "title": "Mnemo: Delete Memory",
        "category": "Mnemo"
      },
      {
        "command": "getmnemo.refresh",
        "title": "Mnemo: Refresh Sidebar",
        "category": "Mnemo",
        "icon": "$(refresh)"
      },
      {
        "command": "getmnemo.setApiKey",
        "title": "Mnemo: Set API Key",
        "category": "Mnemo"
      }
    ],
    "keybindings": [
      {
        "command": "getmnemo.search",
        "key": "ctrl+alt+m",
        "mac": "cmd+alt+m"
      },
      {
        "command": "getmnemo.add",
        "key": "ctrl+alt+a",
        "mac": "cmd+alt+a",
        "when": "editorHasSelection"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "getmnemo-sidebar",
          "title": "Mnemo",
          "icon": "media/sidebar.svg"
        }
      ]
    },
    "views": {
      "getmnemo-sidebar": [
        {
          "id": "getmnemo.recentMemories",
          "name": "Recent Memories",
          "icon": "media/sidebar.svg",
          "contextualTitle": "Mnemo"
        }
      ]
    },
    "menus": {
      "commandPalette": [
        {
          "command": "getmnemo.refresh",
          "when": "false"
        },
        {
          "command": "getmnemo.deleteMemory",
          "when": "false"
        },
        {
          "command": "getmnemo.add",
          "when": "editorHasSelection"
        }
      ],
      "view/title": [
        {
          "command": "getmnemo.refresh",
          "when": "view == getmnemo.recentMemories",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "getmnemo.deleteMemory",
          "when": "view == getmnemo.recentMemories && viewItem == memory",
          "group": "inline"
        }
      ]
    },
    "configuration": {
      "title": "Mnemo",
      "properties": {
        "getmnemo.apiKey": {
          "type": "string",
          "default": "",
          "description": "DEPRECATED — use the 'Mnemo: Set API Key' command. Stored in plaintext on disk; values here will be migrated to VS Code SecretStorage on activation and then cleared.",
          "scope": "machine-overridable",
          "deprecationMessage": "Use the 'Mnemo: Set API Key' command. Plaintext API keys in settings.json will be migrated to SecretStorage automatically."
        },
        "getmnemo.workspaceId": {
          "type": "string",
          "default": "",
          "description": "Workspace ID to scope memory operations to."
        },
        "getmnemo.defaultLimit": {
          "type": "number",
          "default": 10,
          "minimum": 1,
          "maximum": 100,
          "description": "Default number of results to return for searches and the sidebar."
        },
        "getmnemo.endpoint": {
          "type": "string",
          "default": "https://api.getmnemo.dev",
          "description": "Mnemo API endpoint."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -watch -p .",
    "test": "vitest run",
    "lint": "eslint src --ext ts",
    "package": "vsce package",
    "vscode:prepublish": "npm run compile"
  },
  "dependencies": {
    "@mnemo/memory": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^2.24.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
